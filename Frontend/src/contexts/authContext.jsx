
// import { createContext } from 'react';

// export const AuthContext = createContext({});

// const client = axios.create({
//     baseURL: 'http://localhost:3000'
//     // Why?
//         // Login/Signup happens on the Server: Your frontend (React) doesn't check passwords; it sends the credentials to your Node.js/Express server.
//         // The API Endpoint: When you call client.post('/login'), Axios combines the baseURL with the path to find your backend controller.
// })